{{yield}}

{{#if updateBarForm}}
<div>
  <form>
    <h3>Update</h3>
    <div class="form-group">
      {{input value=name id="name" class="form-control" placeholder="Name"}}
    </div>

    <div class="form-group">
      {{input value=location id="location" class="form-control" placeholder="Location"}}
    </div>

    <div class="form-group">
      {{input value=longitude id="longitude" class="form-control" placeholder="Longitude"}}
    </div>

    <div class="form-group">
      {{input value=latitude id="latitude" class="form-control" placeholder="Latitude"}}
    </div>

    <button {{action "updateBar" bar}} class="btn btn-success">Update</button>
    <button {{action 'hideBarForm'}} class="btn btn-warning">Cancel</button>
  </form>
</div>
{{else}}
<button {{action 'barFormShow'}} class="btn btn-primary btn-xs">Update This Bar</button>
{{/if}}
